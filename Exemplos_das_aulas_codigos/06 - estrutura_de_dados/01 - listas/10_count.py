from distutils import core


cores = ["vermelho", "azul", "verde", "azul"]

cores.count("vermelho") # 1 
cores.count("azul") # 2 
cores.count("verde") # 1

# count mostra quantas vezes que tal objeto vai aparecer pra mim.

